/** Cached references to the static canvas elements defined in index.html. */
export const canvas = document.getElementById('canvas') as HTMLElement;
export const svg = document.getElementById('edges') as unknown as SVGSVGElement;
export const empty = document.getElementById('empty') as HTMLElement;
